## Swagger UI start with 
 `http://localhost:8080/swagger-ui/index.html`


# Run local 

Run Mysql  

docker-compose -f docker-compose-mysql.yml up -d ;


Dùng intelliji run Project as spring boot .

